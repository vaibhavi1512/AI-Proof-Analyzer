# MAYA — Phase 1+ core runtime dependencies
# Install: python -m pip install -r requirements.txt
# AI/forensics packages are intentionally deferred to later phases (see README).

Flask==3.1.0
Flask-SQLAlchemy==3.1.1
Flask-Login==0.6.3
SQLAlchemy==2.0.36
python-dotenv==1.0.1
Werkzeug==3.1.3

# Phase 2 — Evidence data engineering (CPU wheels for torch/torchvision)
pillow==11.0.0
pandas==2.2.3
matplotlib==3.9.3
pytest==8.3.4
psutil==6.1.0
# Phase 4.5 — Advanced Explainability (SHAP attributions + image masker)
# shap.maskers.Image requires OpenCV; use headless (no GUI) on CPU/Windows.
shap>=0.44.0
opencv-python-headless>=4.8.0
# Install torch stack with:
#   python -m pip install torch torchvision --index-url https://download.pytorch.org/whl/cpu

# --- Deferred until later product/AI phases ---
# Flask-Login==0.6.3  # installed above
reportlab>=4.0.0
# Phase 5 — Face reference verification
# Detection/embedding uses OpenCV (already installed) by default so MAYA's
# existing torch/torchvision pins are not rewritten. Optional FaceNet engine:
#   python -m pip install facenet-pytorch --no-deps
# then set FACE_VERIFICATION_ENGINE=facenet_pytorch
# (Do not install facenet-pytorch with deps: 2.6.0 pins torch<2.3 / numpy<2.)
